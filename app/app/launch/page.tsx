import { LaunchWizard } from "@/components/LaunchWizard";
import { PageTitle } from "@/components/PageTitle";
export default function LaunchPage() {
  return <><PageTitle eyebrow="Frontend launch simulation" title="LAUNCH YOUR TOKEN" subtitle="Create a token with a public treasury behind it."/><LaunchWizard/></>;
}
