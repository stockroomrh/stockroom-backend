export function ProjectNavigation(_props: { slug: string; active: string }) {
  // Project navigation now lives in the persistent AppShell workspace bar so
  // users never lose their way when moving between Explore and project pages.
  return null;
}
